import { StyleSheet } from 'react-native';

const styledemo = StyleSheet.create({

box: {
    width: '80%',
    height: 150,
    backgroundColor: 'green',
    alignSelf: 'center',
    borderRadius: 9
  }
   
});

export {styledemo}